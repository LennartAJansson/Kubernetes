apiVersion: v1
kind: Service
metadata:
  name: {{ include "cqrs-api.fullname" . }}
  labels:
    {{- include "cqrs-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cqrs-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
